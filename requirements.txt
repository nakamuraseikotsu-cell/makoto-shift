# =============================================================================
# Streamlit Community Cloud デプロイ用 依存パッケージ
# -----------------------------------------------------------------------------
# Streamlit Cloud は `requirements.txt` を自動で `pip install -r` するため、
# 本ファイルにアプリ実行に必要な最小限のパッケージだけを列挙する。
# （scraper.py 等の運用バッチで使う playwright / python-dotenv は除外）
# =============================================================================
streamlit>=1.30,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<3.0
openpyxl>=3.1,<4.0
pulp>=2.7,<3.0
